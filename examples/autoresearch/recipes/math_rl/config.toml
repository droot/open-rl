# Editable Open-RL math RL, inspired by vivekvkashyap/autoresearch-rl.
# Researchers tune only this file. The shared Open-RL API server/trainer stack owns GPU placement.

model = "Qwen/Qwen2.5-0.5B-Instruct"
renderer = "qwen3_instruct"
max_steps = 4
seed = 0
batch_size = 8
rollouts_per_example = 2
max_tokens = 128
temperature = 1.0
lr = 0.000003
loss = "importance_sampling"
eval_enabled = true
eval_interval = 1
